name: typescript action
on:
  push:
    branches: [ "dmp-api" ]
  pull_request:
    branches: [ "dmp-api" ]
  workflow_dispatch:

jobs:
  register_app_dmp:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: ./
      with:
        method: 'POST'