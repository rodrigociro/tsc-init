name: typescript action
on:
  push:
    branches: [ "rest-api" ]
  pull_request:
    branches: [ "rest-api" ]
  workflow_dispatch:
jobs:
  trabajo1:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: ./
      with:
        method: 'GeT'
